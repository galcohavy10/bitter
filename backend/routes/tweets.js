const express = require('express');
const router = express.Router();

// Import your tweet controller functions here
// e.g. const { getAllTweets, createTweet } = require('../controllers/tweets');

// Set up your tweet routes
// e.g. router.get('/', getAllTweets);
// e.g. router.post('/', createTweet);

module.exports = router;
